[project]
name = "indexer-near-lake-framework"
version = "0.1.0"
description = ""
authors = [
    {name = "Near Inc", email = "hello@nearprotocol.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "near-lake-framework>=0.1.3,<0.2.0"
]

[tool.poetry]
package-mode = false

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
